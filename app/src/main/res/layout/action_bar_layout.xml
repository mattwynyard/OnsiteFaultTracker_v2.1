<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:vue="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent">

    <TextView
        android:id="@+id/action_bar_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/title_text_size"
        android:textColor="@color/text_light"
        android:layout_centerInParent="true"
        android:gravity="center_horizontal"
        />
    
    <ImageButton
        android:id="@+id/settings_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/content_desc_settings"
        android:layout_margin="@dimen/settings_icon_margin"
        android:padding="@dimen/action_bar_button_horizontal_margin"
        android:background="@color/transparent"
        android:src="@drawable/setting_button_drawable"
        android:scaleType="centerInside"
        android:visibility="gone"
        />

    <ImageButton
        android:id="@+id/back_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/content_desc_back"
        android:layout_marginTop="@dimen/back_icon_margin"
        android:layout_marginBottom="@dimen/back_icon_margin"
        android:background="@color/transparent"
        android:src="@drawable/back_button_drawable"
        android:scaleType="fitCenter"
        android:visibility="gone"
        />

</RelativeLayout>